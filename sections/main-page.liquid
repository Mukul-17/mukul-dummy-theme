<section class="section">
  <div class="section__head reveal">
    <h1 class="section__title">{{ page.title }}</h1>
  </div>
  <div class="rte" style="padding:0 var(--pad);max-width:780px;margin:0 auto;">{{ page.content }}</div>
</section>

{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
